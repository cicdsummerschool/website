{
  "name": "modern-hugo-theme",
  "version": "1.0.0",
  "description": "A modern, responsive Hugo theme",
  "scripts": {
    "dev": "hugo server -D --bind 0.0.0.0",
    "build": "hugo --minify",
    "clean": "rm -rf public"
  },
  "devDependencies": {
    "hugo-extended": "^0.120.0"
  }
}